# Calculadora (Flex/Bison) con `cbrt()` y `sqrt()`

## Compilar
```bash
make
```

## Ejecutar con archivo
```bash
./calc input.txt
```

## Ejecutar en modo interactivo
```bash
./calc
# luego escribe expresiones y presiona Enter; Ctrl+D para terminar
```
